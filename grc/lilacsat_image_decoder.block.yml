# auto-generated by grc.converter

id: lilacsat_image_decoder
label: Image decoder
category: '[lilacsat]'

parameters:
-   id: path
    label: Path
    dtype: string
-   id: display
    label: Display
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: message
    id: in

templates:
    imports: import lilacsat
    make: lilacsat.image_decoder(${path}, ${display})

file_format: 1
